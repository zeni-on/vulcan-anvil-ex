# 보안 기준선

> 상태: 초안 v0.1
> 목적: 요구사항, 설계, 구현, 테스트 단계에서 반복적으로 참조할 공통 보안 기준을 정의한다.

본 문서는 공통 기준선이다. 프로젝트별 실제 구현 값, 입력 정책, 오류 메시지, 적용 위치, 테스트 ID는 Gate 2의 보안가이드(`docs/artifacts/02-design/security/DOC-SEC-G2-001_Security-Guide_v0.1.md`)에 확정한다.

## 1. 기본 원칙

에이전트는 보안 설계가 필요할 때마다 원문 PDF 전체를 매번 읽지 않는다.

대신 다음 순서로 판단한다.

1. 본 문서에서 기능 유형별 필수 보안항목을 식별한다.
2. 식별된 항목을 `SEC-ID`, `KISA/SR`, `OWASP`, `CWE`, `TEST-ID`와 연결한다.
3. 세부 판단이 필요할 때만 `docs/seed-docs/reference-standards/`의 원문을 확인한다.
4. 원문과 본 문서가 충돌하면 원문을 우선하고, 본 문서 갱신 후보를 `FIND` 또는 `CR`로 남긴다.

본 문서는 다음 기준을 기본 축으로 사용한다. 적용 강도는 Delivery Profile에 따라 다르다.

| 기준 | 역할 | Ex 사용 방식 |
| --- | --- | --- |
| OWASP ASVS | 웹 애플리케이션 보안 요구사항과 검증 기준 | Product/Audit의 기본 보안 체크리스트 |
| OWASP Top 10 | 웹 애플리케이션 위험 분류 | 위험 유형 및 누락 체크 |
| OWASP API Security Top 10 | API 보안 위험 분류 | API 중심 앱의 접근통제, 인증, 리소스 사용 위험 점검 |
| CWE | 코드 레벨 보안약점 분류 | 구현/리뷰/진단 시 약점 ID 연결 |
| 행안부/KISA `SR-ID` | 국내 공공 개발보안 및 감리 대응 기준 | Audit profile 또는 고객/공공 기준이 필요한 경우 1차 근거 |

Profile별 기본 적용은 다음과 같다.

| Profile | 기본 보안 기준 |
| --- | --- |
| `poc` | 주요 위험 식별, 민감정보/인증/외부입력 여부, 제품화 전환 시 보강할 gap |
| `product` | OWASP ASVS, OWASP Top 10, OWASP API Security Top 10, CWE 기반 제품 보안 기준선 |
| `audit` | Product 기준선 + KISA/공공/고객사 기준 매핑 + 제출/검수용 증적 |

## 2. 참조 표준

| 참조 코드 | 문서/표준 | 위치 또는 URL | 용도 |
| --- | --- | --- | --- |
| `KISA-SD-2021` | 소프트웨어 개발보안 가이드(2021.12.29) | `docs/seed-docs/reference-standards/` | 설계/구현 보안 기준 |
| `KISA-SWVD-2021` | 소프트웨어 보안약점 진단가이드(2021) | `docs/seed-docs/reference-standards/` | 감리/진단/검증 기준 |
| `OWASP-ASVS` | OWASP Application Security Verification Standard | `https://owasp.org/www-project-application-security-verification-standard/` | 웹 애플리케이션 보안 요구사항/검증 기준 |
| `OWASP-T10-2021` | OWASP Top 10:2021 | `https://owasp.org/Top10/2021/` | 웹 위험 분류 |
| `OWASP-API-T10-2023` | OWASP API Security Top 10:2023 | `https://owasp.org/www-project-api-security/` | API 보안 위험 분류 |
| `CWE` | Common Weakness Enumeration | `https://cwe.mitre.org/` | 코드 약점 분류 |

## 3. 보안항목 작성 규칙

`SEC-ID`는 내부 추적 ID이며, 외부 기준 ID를 반드시 함께 가진다.

권장 형식:

```yaml
security_id: SEC-001
name: 인증 실패 처리
description: 인증 실패 시 세션을 생성하지 않고 계정 존재 여부를 노출하지 않는다.
applies_to:
  - REQ-001-02
  - PGM-001
  - SCR-001
reference_standards:
  kisa_sr:
    - KISA-SD-2021 SR2-1
    - KISA-SD-2021 SR2-2
  owasp:
    - OWASP-T10-2021 A07
  cwe:
    - CWE-287
    - CWE-209
verification:
  - UT-002
  - IT-001
```

필수 규칙:

- 인증, 권한, 비밀번호, 세션, 개인정보, 파일, 외부 입력, 외부 연계가 있으면 `SEC-ID` 후보를 만든다.
- `SEC-ID`는 최소 하나 이상의 `REQ` 또는 `NREQ`와 연결한다.
- `SEC-ID`는 최소 하나 이상의 `PGM`, `SCR`, `DB`, `IF` 중 하나와 연결한다.
- `SEC-ID`는 최소 하나 이상의 테스트 또는 리뷰 증적과 연결한다.
- Product profile에서는 OWASP ASVS/Top 10/API Top 10과 CWE를 먼저 작성하고, KISA/SR은 필요할 때 참고 또는 Audit 전환 gap으로 남긴다.
- 공공/SI/Audit profile에서는 `KISA/SR` 매핑을 먼저 작성하고, OWASP/CWE는 보조 분류로 추가한다.

## 4. 기능 유형별 필수 보안항목

### 4.1 인증/로그인

로그인, 사용자 식별, 비밀번호 검증, 세션 발급 기능이 있으면 다음 항목을 검토한다.

| SEC 후보 | 보안항목 | KISA/SR | OWASP | CWE | 요구사항/설계 반영 | 테스트 예 |
| --- | --- | --- | --- | --- | --- | --- |
| `SEC-AUTH-001` | 인증 대상 및 방식 정의 | `SR2-1` | `A07` | `CWE-287` | 로그인 대상, 인증 방식, 우회 방지 조건 | 정상/실패 로그인 |
| `SEC-AUTH-002` | 인증 실패 처리 및 계정 존재 여부 미노출 | `SR2-1`, `SR3-1` | `A07`, `A05` | `CWE-209`, `CWE-287` | 실패 메시지, 실패 응답, 세션 미생성 | 실패 로그인 |
| `SEC-AUTH-003` | 로그인 시도 제한 | `SR2-2` | `A07` | `CWE-307` | 실패 횟수 제한, 잠금, 감사 로그 | 반복 실패 |
| `SEC-AUTH-004` | 비밀번호 정책 | `SR2-3` | `A07` | `CWE-521` | 길이, 복잡도, 금지 패턴, 변경 정책 | 약한 비밀번호 거부 |
| `SEC-AUTH-005` | 비밀번호 저장 보호 | `SR2-3`, `SR2-6`, `SR2-7` | `A02`, `A07` | `CWE-256`, `CWE-916` | 평문 저장 금지, salt/반복 기반 해시 | 저장값 검증 |
| `SEC-AUTH-006` | 세션 토큰 보호 | `SR4-1`, `SR2-8` | `A01`, `A07` | `CWE-384`, `CWE-613` | 세션 생성, 만료, 재사용 방지, 전송 보호 | 세션 접근 |

### 4.2 권한/접근통제

로그인 사용자와 비로그인 사용자, 관리자와 일반 사용자, 소유자와 타 사용자를 구분하면 다음 항목을 검토한다.

| SEC 후보 | 보안항목 | KISA/SR | OWASP | CWE | 요구사항/설계 반영 | 테스트 예 |
| --- | --- | --- | --- | --- | --- | --- |
| `SEC-AC-001` | 중요 기능 접근통제 | `SR2-4` | `A01` | `CWE-862` | 기능별 권한 조건 | 비로그인 차단 |
| `SEC-AC-002` | 객체 단위 권한 검증 | `SR2-4` | `A01` | `CWE-639` | 게시글/첨부/사용자별 소유권 | 타 사용자 자원 접근 |
| `SEC-AC-003` | 보안기능 입력값 검증 | `SR1-9` | `A01`, `A03` | `CWE-20` | 권한 판단에 쓰는 파라미터 검증 | role/userId 변조 |

### 4.3 입력값 검증 및 출력 인코딩

검색, 등록, 수정, 삭제, URL 파라미터, JSON body, 파일명, HTML 출력이 있으면 다음 항목을 검토한다.

| SEC 후보 | 보안항목 | KISA/SR | OWASP | CWE | 요구사항/설계 반영 | 테스트 예 |
| --- | --- | --- | --- | --- | --- | --- |
| `SEC-IN-001` | 일반 입력값 검증 | `SR1-1`, `SR1-9` | `A03` | `CWE-20` | 필수값, 길이, 형식, 허용값 | 잘못된 입력 |
| `SEC-IN-002` | SQL/DB 조회 입력 검증 | `SR1-1` | `A03` | `CWE-89` | 바인딩, 정적 쿼리, ORM 안전 사용 | SQL injection |
| `SEC-IN-003` | 웹 출력 인코딩 | `SR1-5` | `A03` | `CWE-79` | HTML/JS/URL 컨텍스트별 인코딩 | XSS |
| `SEC-IN-004` | URL/Redirect 검증 | `SR1-7` | `A01`, `A05` | `CWE-601` | 허용 URL, redirect 대상 제한 | Open redirect |

### 4.4 파일 업로드/다운로드

첨부파일, 이미지, 엑셀 업로드, 다운로드가 있으면 다음 항목을 검토한다.

| SEC 후보 | 보안항목 | KISA/SR | OWASP | CWE | 요구사항/설계 반영 | 테스트 예 |
| --- | --- | --- | --- | --- | --- | --- |
| `SEC-FILE-001` | 업로드 파일 검증 | `SR1-10` | `A05` | `CWE-434` | 확장자, MIME, 크기, 악성 파일 검사 | 허용 외 파일 |
| `SEC-FILE-002` | 다운로드 경로 검증 | `SR1-4`, `SR1-10` | `A01` | `CWE-22` | 경로 조작 방지, 파일 소유권 | Path traversal |
| `SEC-FILE-003` | 파일 실행 방지 | `SR1-10`, `SR2-4` | `A05` | `CWE-434` | 저장 위치, 실행 권한 차단 | 업로드 실행 시도 |

### 4.5 중요정보/개인정보

개인정보, 인증정보, 식별정보, 토큰, API Key, 비밀키가 있으면 다음 항목을 검토한다.

| SEC 후보 | 보안항목 | KISA/SR | OWASP | CWE | 요구사항/설계 반영 | 테스트 예 |
| --- | --- | --- | --- | --- | --- | --- |
| `SEC-DATA-001` | 중요정보 저장 보호 | `SR2-7` | `A02` | `CWE-312` | 암호화/마스킹/접근통제 | 저장값 확인 |
| `SEC-DATA-002` | 중요정보 전송 보호 | `SR2-8` | `A02` | `CWE-319` | TLS, 쿠키 Secure/HttpOnly | 전송 설정 |
| `SEC-DATA-003` | 로그 민감정보 출력 금지 | `SR3-1` | `A09` | `CWE-532` | 로그 항목, 마스킹 | 로그 검토 |
| `SEC-DATA-004` | 암호키 관리 | `SR2-5` | `A02` | `CWE-798`, `CWE-321` | 키 저장소, 환경변수, 회전 | 키 하드코딩 점검 |

### 4.6 API/외부 연계

외부 API, 웹훅, 내부 시스템 연계, 배치 연계가 있으면 다음 항목을 검토한다.

| SEC 후보 | 보안항목 | KISA/SR | OWASP | CWE | 요구사항/설계 반영 | 테스트 예 |
| --- | --- | --- | --- | --- | --- | --- |
| `SEC-API-001` | API 인증/인가 | `SR2-1`, `SR2-4` | `A01`, `A07` | `CWE-862` | 토큰, 권한, 호출자 식별 | 무권한 호출 |
| `SEC-API-002` | 요청/응답 검증 | `SR1-5`, `SR1-9` | `A03` | `CWE-20` | 스키마, 필수값, 허용값 | 잘못된 payload |
| `SEC-API-003` | SSRF/외부 URL 제한 | `SR1-7` | `A10` | `CWE-918` | 허용 도메인, 내부망 차단 | 내부 URL 호출 |

### 4.7 설정/배포/운영

환경변수, 설정파일, 빌드/배포, 기본 계정, 디버그 모드가 있으면 다음 항목을 검토한다.

| SEC 후보 | 보안항목 | KISA/SR | OWASP | CWE | 요구사항/설계 반영 | 테스트 예 |
| --- | --- | --- | --- | --- | --- | --- |
| `SEC-CONF-001` | 기본 계정/비밀번호 금지 | `SR2-1`, `SR2-3` | `A05`, `A07` | `CWE-1392`, `CWE-1393` | 초기 계정 정책 | 기본 계정 점검 |
| `SEC-CONF-002` | 디버그/오류 정보 노출 방지 | `SR3-1` | `A05` | `CWE-209`, `CWE-489` | 오류 응답, 운영 설정 | 오류 응답 점검 |
| `SEC-CONF-003` | 취약한 구성요소 관리 |  | `A06` | `CWE-1104` | 의존성 점검, 버전 관리 | dependency scan |

## 5. Gate별 적용 규칙

| Gate | 필수 수행 |
| --- | --- |
| P0 | 보안 민감 도메인, 개인정보, 외부연계, 인증/권한 여부를 질문으로 남긴다. |
| G1 | 기능 유형별 필수 보안항목을 `NREQ`, `SEC`, `AC` 후보로 식별한다. |
| G2 | 보안가이드에서 Profile별 보안 근거, 프로젝트 상세스펙, OWASP/CWE 분류를 확정하고 프로그램 설계, 화면설계, DB명세에 반영한다. Audit은 `KISA/SR` 근거를 포함한다. |
| G3 | 모든 `SEC-ID`의 구체 규격에 보안 테스트 또는 리뷰 증적 기준을 연결한다. |
| G4 | 구현 코드가 보안 기준을 만족하는지 확인하고, 미흡하면 `FIND` 또는 `CR`로 분류한다. |
| G5 | 미해결 보안 Finding, 보안 테스트 미실행, CR 승격 대상을 확인한다. |

## 6. 보안 요구사항 식별 체크리스트

요구사항 persona와 security-review persona는 다음 질문에 하나라도 Yes가 있으면 `SEC-ID` 후보를 작성한다.

| 질문 | Yes일 때 후보 |
| --- | --- |
| 로그인이 있는가 | `SEC-AUTH-001` ~ `SEC-AUTH-006` |
| 비밀번호를 입력, 변경, 저장하는가 | `SEC-AUTH-004`, `SEC-AUTH-005` |
| 로그인 실패를 처리하는가 | `SEC-AUTH-002`, `SEC-AUTH-003` |
| 사용자별 권한이 다른가 | `SEC-AC-001`, `SEC-AC-002` |
| 사용자 입력을 DB 조회나 저장에 사용하는가 | `SEC-IN-001`, `SEC-IN-002` |
| HTML 화면에 사용자 입력을 출력하는가 | `SEC-IN-003` |
| 파일 업로드/다운로드가 있는가 | `SEC-FILE-001` ~ `SEC-FILE-003` |
| 개인정보나 인증정보를 저장/전송하는가 | `SEC-DATA-001`, `SEC-DATA-002` |
| 외부 URL이나 외부 API를 호출하는가 | `SEC-API-001` ~ `SEC-API-003` |
| 운영 설정, 기본 계정, 디버그 모드가 있는가 | `SEC-CONF-001`, `SEC-CONF-002` |

## 7. 설계 산출물 반영 위치

| 산출물 | 필수 반영 |
| --- | --- |
| 요구사항정의서 | 보안 요구사항, 비기능 요구사항, 보안 인수기준 |
| 요구사항추적표 | `SEC-ID`, Profile별 보안 기준, `OWASP`, `CWE`, 테스트/증적. Audit은 `KISA/SR` 포함 |
| 보안가이드 | `SEC-ID`별 구현 규격, 정책값, 오류/메시지, 적용 위치, 검증 ID |
| 기능명세서 | 기능별 보안 제약과 예외 흐름 |
| 화면설계서 | 인증/권한 상태, 오류 메시지, 입력값 검증, 민감정보 표시 여부 |
| 프로그램 설계서 | 입력 검증, 인증/인가, 암호, 세션, 오류 처리 구현 규칙 |
| DB명세서 | 중요정보/개인정보 여부, 저장 보호, 로그 출력 금지 |
| 개발표준정의서 | 언어별 금지 패턴, 권장 라이브러리, 메시지/로그 규칙 |
| 테스트케이스 | `SEC-ID`별 보안 테스트, 미실행 사유, 증적 기준 |
| QA 발견사항 | 보안 기준 미충족, 취약 구현, 누락된 보안 요구사항 |

## 8. 샘플: 로그인 게시판 적용

로그인 기능이 있는 게시판은 최소 다음 보안항목을 가져야 한다.

| SEC-ID | 보안항목 | KISA/SR | OWASP | CWE | 관련 테스트 |
| --- | --- | --- | --- | --- | --- |
| `SEC-001` | 인증 실패 처리 및 계정 존재 여부 미노출 | `SR2-1`, `SR3-1` | `A07`, `A05` | `CWE-287`, `CWE-209` | `UT-002`, `IT-001` |
| `SEC-002` | 비로그인 게시글 작성 차단 | `SR2-4` | `A01` | `CWE-862` | `UT-003`, `IT-002` |
| `SEC-003` | 비밀번호 정책 | `SR2-3` | `A07` | `CWE-521` | `UT-004` |
| `SEC-004` | 비밀번호 저장 보호 | `SR2-3`, `SR2-6`, `SR2-7` | `A02`, `A07` | `CWE-256`, `CWE-916` | `UT-005` |
| `SEC-005` | 세션 토큰 보호 | `SR4-1`, `SR2-8` | `A01`, `A07` | `CWE-384`, `CWE-613` | `IT-003` |
| `SEC-006` | 게시글 입력값 검증 및 출력 인코딩 | `SR1-1`, `SR1-5` | `A03` | `CWE-20`, `CWE-79` | `UT-006`, `IT-004` |

## 9. 미해결 또는 주의사항

- OWASP Top 10과 CWE는 계속 개정될 수 있으므로 프로젝트 시작 시 기준 버전을 확정한다.
- KISA `SR-ID`는 설계단계 보안설계 기준이다. 구현단계 세부 보안약점은 `KISA-SWVD-2021`과 언어별 시큐어코딩 가이드를 추가 확인한다.
- 비밀번호 저장 보호는 단순 해시가 아니라 salt와 충분한 반복/전용 알고리즘 적용 여부를 검토해야 한다.
- 본 문서는 기준선이며, 프로젝트 도메인 특화 보안 요구사항은 별도 `SEC-ID`로 추가한다.

