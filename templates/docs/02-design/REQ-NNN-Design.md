# REQ-NNN 설계 문서

> 생성일: {{GENERATED_DATE}}
> 이 문서는 Gate 2에서 Architect 에이전트가 작성합니다.
> 파일명을 `REQ-001-Design.md` 형식으로 변경한 후 내용을 채우세요.

## 개요

- **REQ 그룹**: REQ-NNN — [그룹명]
- **설계 방식**: [MVC / Layered / Clean Architecture / ...]
- **핵심 결정사항**: [1~2문장으로 이 설계의 핵심 선택을 기술]

## 시스템 구조

```
[Mermaid 다이어그램 또는 텍스트 구조도]
```

## 모듈/컴포넌트 설계

> 각 모듈의 책임, 공개 인터페이스, 제약조건을 기술한다. 내부 구현 코드는 작성하지 않는다.

### [모듈명]

- **책임**: [이 모듈이 하는 일]
- **인터페이스**: [함수 시그니처 또는 props 타입]
- **제약**: [기술적 제약사항 및 이유]

## API 설계

| Method | Path | 설명 | 인증 | 요청 바디 | 응답 |
|--------|------|------|------|-----------|------|
| GET | /api/v1/... | ... | — | — | `{ ... }` |
| POST | /api/v1/... | ... | Bearer | `{ ... }` | `{ ... }` |

## 디렉토리 구조

```
src/
├── [모듈A]/
│   ├── [파일].ts       — [역할]
│   └── [파일].ts       — [역할]
└── [모듈B]/
```

## 데이터 흐름

```
[사용자 액션] → [컴포넌트] → [API 호출] → [서버 처리] → [DB] → [응답]
```

## 에러 처리 방침

| 케이스 | HTTP 코드 | 처리 방식 |
|--------|----------|---------|
| 인증 실패 | 401 | [처리 방식] |
| 권한 없음 | 403 | [처리 방식] |
| 리소스 없음 | 404 | [처리 방식] |

## 보안 고려사항

| SEC-ID | 위협 | 대응 방안 |
|--------|------|---------|
| SEC-001 | [위협 유형] | [대응 방안] |

## 단위 테스트 ID 사전 할당

| UT-ID | 대상 모듈 | 테스트 시나리오 | 담당 |
|-------|---------|--------------|------|
| UT-NNN-01 | [모듈명] | [시나리오] | Developer |
| UT-NNN-02 | [모듈명] | [시나리오] | Developer |

## 기술 선택 근거

| 선택 | 대안 | 선택 이유 |
|------|------|---------|
| [선택한 기술] | [대안 기술] | [이유] |

## 미결 사항 / 트레이드오프

- [ ] [결정이 필요한 사항 또는 알려진 트레이드오프]
